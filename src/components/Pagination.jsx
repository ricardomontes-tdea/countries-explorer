import "../styles//Pagination.css";

export const Pagination = ({ currentPage, totalPages, onPageChange }) => {
  return (
    <div className="pagination">
      <p style={{ color: "gray" }}> ** Paginación pendiente de implementar **</p>
    </div>
  );
};
